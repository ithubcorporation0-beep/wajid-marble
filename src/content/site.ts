// This file will hold all of the site's real words: headings, paragraphs,
// contact details, product names and so on — so that anyone editing the
// text on the site only ever needs to open this one file, never the
// component files that control layout and markup. It is intentionally empty
// for now; content is added in a later step.

export {};
