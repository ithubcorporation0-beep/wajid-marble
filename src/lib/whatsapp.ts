// This file will build the WhatsApp links and pre-filled messages the site
// uses to hand a visitor's quote request over to WhatsApp (the "wa.me"
// links seen in the reference design). Keeping this logic in one file means
// the phone number and message wording only need to be correct in one
// place. The real link/message building is added once the quote form's
// fields are finalized.

export {};
