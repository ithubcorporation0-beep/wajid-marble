// This renders one procedurally-generated marble/stone texture swatch (an
// SVG turbulence effect, like the ones used as placeholder backgrounds for
// product and gallery cards in the reference design) instead of a real
// photo. It has no markup yet — the actual SVG generation is added in a
// later step.

export default function MarbleSwatch() {
  return null;
}
