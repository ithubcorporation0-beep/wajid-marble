// This is the thin gold decorative line (styled by .vein-divider in
// src/styles/primitives.css) used to separate sections, meant to look like a
// vein running through a slab of marble. It has no markup yet — the actual
// SVG line is added in a later step.

export default function VeinDivider() {
  return null;
}
