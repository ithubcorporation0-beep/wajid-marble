// This is the full-screen navigation menu that slides in on small screens
// when a visitor taps the hamburger button in SiteHeader. It has no markup
// or content yet, and the open/close behavior isn't wired up — those are
// added in a later step.

export default function MobileDrawer() {
  return null;
}
