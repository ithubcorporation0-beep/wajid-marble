// This is the footer shown at the bottom of every page: the site name,
// footer links, and copyright line. It has no markup or content yet — that's
// added in a later step.

export default function SiteFooter() {
  return null;
}
