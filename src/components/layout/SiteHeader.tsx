// This is the site's fixed header: the logo, the top navigation links, and
// the "Call" / "Get a Quote" buttons that stay pinned to the top of every
// page as visitors scroll. It has no markup or content yet — that's added in
// a later step, along with the mobile hamburger button that opens
// MobileDrawer.

export default function SiteHeader() {
  return null;
}
