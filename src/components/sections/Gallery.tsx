// This is the "From the Factory Floor" section: the mixed-size photo grid
// showing slabs, cuts and finished work. It has no markup or content yet —
// that's added in a later step.

export default function Gallery() {
  return null;
}
