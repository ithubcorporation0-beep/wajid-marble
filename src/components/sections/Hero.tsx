// This is the full-height introduction visitors see first: the headline,
// short intro text, and the "Request a Quote" / "Chat on WhatsApp" buttons
// over a marble-textured background. It has no markup or content yet —
// that's added in a later step.

export default function Hero() {
  return null;
}
