// This is the "What We Offer" section: the grid of stone product cards
// (marble types, granite, etc). It has no markup or content yet — that's
// added in a later step.

export default function Products() {
  return null;
}
