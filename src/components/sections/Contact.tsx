// This is the "Get In Touch" section: the contact methods (call, WhatsApp,
// location) alongside the quote request form (QuoteForm). It has no markup
// or content yet — that's added in a later step.

export default function Contact() {
  return null;
}
