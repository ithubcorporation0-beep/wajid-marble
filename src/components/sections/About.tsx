// This is the "About the Factory" section: the introduction text plus the
// owner's info card with stats. It has no markup or content yet — that's
// added in a later step.

export default function About() {
  return null;
}
