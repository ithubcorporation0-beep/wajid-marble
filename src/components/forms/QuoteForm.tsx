// This is the "Request a Quote" form used inside the Contact section: name,
// phone, stone type and project details. It has no fields or content yet.
// Once built, it will validate input with src/lib/schemas.ts and submit to
// the /api/quote route.

export default function QuoteForm() {
  return null;
}
