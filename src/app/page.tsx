// This is the homepage — what visitors see at the site's root address ("/").
// It will assemble the page sections (hero, about, products, gallery, why
// us, contact) in order. Left empty for now: the sections themselves and
// their content are built in a later step.

export default function HomePage() {
  return null;
}
