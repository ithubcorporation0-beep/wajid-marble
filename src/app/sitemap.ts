// This file generates the site's sitemap.xml automatically — a list of pages
// that tells search engines like Google what exists on the site. Next.js
// serves whatever this function returns at the address /sitemap.xml. The
// list of real page addresses is filled in once those pages exist.
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  return [];
}
