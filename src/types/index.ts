// This file is the shared home for TypeScript types used in more than one
// place in the app (for example, the shape of a product or a quote
// request). Nothing lives here yet — types are added as each feature is
// built.

export {};
