// This file configures Next.js itself (the framework that builds and serves
// the site). It is intentionally close to the framework's defaults — options
// are only added here once a specific need for them comes up.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
